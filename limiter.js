"use strict";

const TokenBucket = require("limiter").TokenBucket;

const removeTokens = function(count, callback) {

};

const tryRemoveTokens = function(count) {

};

module.exports = {
  removeTokens: removeTokens,
  tryRemoveTokens: tryRemoveTokens
}
